Algoritmo "intervalo"

Var
   n1, contador,a,b,c: inteiro

Inicio
   contador <- 0
a <- 1
b <- -1

   Escreva ("Digite um número inteiro!")
   leia (n1)
   Enquanto (n1 <> contador ) faca
   contador <- contador + 1
   c <- b + a
   b <- a
   a <- c
   escreva (c)
   fimenquanto

Fimalgoritmo